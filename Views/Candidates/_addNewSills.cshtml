@model Skill
<div class="d-flex justify-content-between mt-2">
    <div class="col-10">
        @Html.DropDownListFor(x=>x.SkillId,ViewBag.skill as SelectList, "-----Select----",new{@class="form-select"})
    </div>
    <div class="col-2">
        <a id="btnDeleteSkill" class="btn btn-danger btn-sm">Remove</a>
    </div>
</div>